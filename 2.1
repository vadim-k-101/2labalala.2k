#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int main(void) {
  char line[] = "";
  fgets(line, 1000, stdin);

  puts(line);
  for(int i = 0; i < strlen(line) - 1; i++){
    line[i] = tolower(line[i]);
  }

  puts(line);

  return 0;
}
